import { InnerWayPointType } from '~/types';
import { IconName } from '~/primitives/Icon';

export const ICONS: Record<InnerWayPointType, IconName> = {
  energy: 'lightning',
  entertainment: 'star',
  hydration: 'drop',
  timing: 'clock',
  restrooms: 'toilet',
};
